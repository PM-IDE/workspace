// Original file: ../../../../../protos/util.proto

import type { Long } from '@grpc/proto-loader';

export interface GrpcTimeSpan_DONTUSE {
  'nanoseconds'?: (number | string | Long);
}

export interface GrpcTimeSpan {
  'nanoseconds': (number);
}
